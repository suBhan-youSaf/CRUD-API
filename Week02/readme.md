# Restaurant API - Stage 0: Hello Server

Before a restaurant can serve food, the doors have to open. This stage opens the doors by spinning up a local development server.

## Prerequisites

Ensure you have your lane's runtime installed (Node.js or Python) as per the course resources.

## Quick Start

### Option A: Node.js (Express Lane)
1. Install dependencies:
   ```bash
   npm install express
